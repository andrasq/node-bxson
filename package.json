{
  "name": "bxson",
  "description": "compact json-like binary serialization",
  "version": "0.0.4",
  "main": "bjson",

  "scripts": {
    "test": "qnit test-*",
    "coverage": "nyc -r text -r lcov qnit test-*",
    "clean": "rm -rf .nyc_output coverage npm-debug.log"
  }
}
