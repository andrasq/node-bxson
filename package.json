{
  "name": "bxson",
  "description": "fast compact json-like binary serialization",
  "keywords": ["compact", "fast", "binary", "json"],
  "version": "0.0.5",
  "main": "bjson",
  "license": "Apache-2.0",
  "files": ["README.md", "bjson.js", "pushbuf.js"],

  "scripts": {
    "test": "qnit test-*",
    "coverage": "nyc -r text -r lcov npm test",
    "clean": "rm -rf .nyc_output coverage npm-debug.log"
  }
}
